# Database and generated secret — never committed
data/

# The shop's photography IS committed: it is the deployed read-only image
# store, and without it a hosted preview renders with no product imagery.
# The multi-megabyte masters stay out.
uploads/_originals/

# Environment
.env
.env.*
!.env.example

node_modules/
npm-debug.log*
.DS_Store
Thumbs.db
